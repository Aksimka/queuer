import { FC, ReactElement } from 'react'
import classes from './Description.module.css'

const Description: FC = (): ReactElement => {
  return <div className={classes.description}>description page</div>
}

export default Description
